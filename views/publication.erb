<!DOCTYPE html>
<meta charset="utf-8">
<html>
<head>
  <title>Eventbrite</title>

  <style type="text/css">
    body {
      background: #fff;
      color: #000;
      width: 384px;
      padding: 0;
      margin: 0;
      font-family: Arial, sans-serif;
    }
    h1, h2, h3, h4, p, li {
      margin: 0 0 12px 0;
      padding: 0;
      font-size: 20px;
      line-height: 24px;
    }
    .pub-header {
      background: url(<%= url('/img/top_rule.png') %>) 0 0 no-repeat; 
      padding-top: 18px;
    }
      .pub-title {
        font-weight: normal;
        padding-left: 70px;
        background: url(<%= url('img/eventbrite_icon.png') %>) 16px 3px no-repeat;
      }
    .event {
      margin-bottom: 24px;
    }
      .event .event-title,
      .event p {
        padding-left: 15px;
        padding-right: 15px;
      }
      .event-header {
        color: #fff;
        padding-top: 10px;
        padding-bottom: 8px;
        background: #000 url(<%= url('/img/scalloped_edge.png') %>) 0 100% no-repeat;
        margin-bottom: 6px;
      }
        .event-title {
          font-size: 28px;
          line-height: 32px;
          margin-bottom: 4px;
          font-weight: normal;
        }
      .event-time span {
        white-space: nowrap;
      }
      .event-footer {
          border-top: 2px solid #000;
          padding-top: 12px;
          background: url(<%= url('/img/diagonal_lines.png') %>) 0 0 repeat-y;
          padding-bottom: 12px;
          border-bottom: 2px solid #000;
      }
        .event-url {
          font-size: 16px;
          background: #fff;
          margin-bottom: 0;
        }
    .events-list-title {
      padding-left: 14px;
      margin-top: -10px;
      margin-bottom: 0;
    }
    .events-list {
      margin: 0 0 25px 0;
      padding: 0 10px;
    }
      .events-list-item {
        padding: 0 0 0 15px;
        margin: 0 0 0 4px;
        background: url(<%= url('/img/bullet.png') %>) 0 9px no-repeat;
        list-style-type: none;
        font-weight: bold;
      }

  </style>

</head>
<body>

  <header class="pub-header">
    <h1 class="pub-title">
      <%= @user['first_name'] %> <%= @user['last_name'] %>:
      Your events tomorrow from Eventbrite
    </h1>
  </header>

  <% @events_to_show_in_full = 3 %>

  <% @all_events.each_with_index do |ev, n| %>
    <%= erb :partial_event, :locals => {:ev => ev, :count => n} %>
  <% end %>

  <% if @all_events.length > @events_to_show_in_full %>
    </ul>
  <% end %>

</body>
</html>
