<!DOCTYPE html>
<meta charset="utf-8">
<html>
<head>
  <title>Eventbrite</title>

  <style type="text/css">
    body {
      background: #fff;
      color: #000;
      width: 384px;
      padding: 0;
      margin: 0;
      font-family: Arial, sans-serif;
      font-size: 18px;
    }
    h1, h2, h3, p {
      margin: 0 0 10px 0;
      line-height: 22px
    }
    h1 {
      font-size: 24px;
      line-height: 28px;
      margin-bottom: 20px;
      border: 2px solid #000;
      border-radius: 8px;
      padding: 7px 10px 8px 10px;
    }
    h2 {
      font-size: 18px;
      line-height: 22px;
      padding: 0 12px;
    }
    .event {
      border-radius: 8px;
      border: 2px solid #000;
      padding-top: 10px;
      margin-bottom: 20px;
    }
      .event .event-title,
      .event p {
        padding-left: 10px;
        padding-right: 10px;
      }
      .event-title {
        margin: 0;
        padding: 0;
        font-size: 24px;
        line-height: 28px;
      }
      .event-org {
        border-bottom: 2px solid #000;
        padding-bottom: 10px;
      }
      .event-time span {
        white-space: nowrap;
      }
      .event-url {
        border-top: 2px solid #000;
        padding-top: 10px;
        font-size: 16px;
      }

  </style>

</head>
<body>

  <h1>
    Eventbrite Reminder for<br />
    <%= @user['first_name'] %> <%= @user['last_name'] %>
  </h1>

  <% if @events.length > 0 %>
    <h2>
      <% if @events.length == 1 %>
        You have an event tomorrow!
      <% else %>
        <%= format_number_text(@events.length).capitalize %> of your events are tomorrow!
      <% end %>
    </h2>

    <% @events.each do |ev| %>
      <%= erb :partial_event, :locals => {:ev => ev} %>
    <% end %>
  <% end %>

  <% if @tickets.length > 0 %>
    <h2>
      <% if @tickets.length == 1 %>
        You have a ticket for tomorrow!
      <% else %>
        You have <%= format_number_text(@tickets.length) %> tickets for tomorrow!
      <% end %>
    </h2>

    <% @tickets.each do |tk| %>
      <%= erb :partial_event, :locals => {:ev => tk['event']} %>
    <% end %>
  <% end %>

</body>
</html>
